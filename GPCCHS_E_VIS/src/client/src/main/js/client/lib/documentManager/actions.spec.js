import {} from '../common/test';
// import { openView } from './openView';

describe('documentManager:actions', () => {
  describe('openView', () => {
    it('dispatches a message when error');
    it('dispatches a loadDocuments when view is loaded');
  });
});
