const commands = {
  main: {
    bind: () => {},
    rpc: (event, payload, callback) => {},
    message: (event, payload) => {},
    getSessions: () => {},
    getSessionTime: () => {},
  },
};

export default commands;
