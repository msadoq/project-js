package dataControllerUtils.protobuf;

message AlarmMode {
extensions 2 to 102;
    enum ALARM_MODE {
        ALL = 0;
        NON_NOMINAL = 1;
        WITH_ACK = 2;
    }

	required ALARM_MODE mode = 1; 
}
