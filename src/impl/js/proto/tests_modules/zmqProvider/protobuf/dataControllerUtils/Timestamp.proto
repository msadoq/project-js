package dataControllerUtils.protobuf;


//picoseconds are here just in case it is required to filter on ps.

message Timestamp {
extensions 5 to 107;
  required uint64 ms = 1;
  optional uint32 ps = 2;
}