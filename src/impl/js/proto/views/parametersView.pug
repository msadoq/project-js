extends layout

block content
  script(src="/socket.io/socket.io.js")
  script(type='text/javascript').
    var socket = io.connect('http://localhost:1337');
    var firstLineId;
    socket.on('message', function(message) {
        $("#message").append(message);
    })
    socket.on('Parameters', function(message) {
        $("#ui").empty();
        //message.parameters.forEach(function(item){
            $("#ui").append("<tr> <td>"+message.onboardDate+"</td><td>"+message.extractedValue+"</td><td>"+message.rawValue+"</td><td>"+message.convertedValue+"</td></tr>");
        //})

    })
  div.top(id="message")
  div.container
    div.content
      table.table.table-striped
        thead
          tr
            th onboard Date
            th Extracted Value
            th Raw Value
            th Converted Value
        tbody(id="ui")
 
  div.footer