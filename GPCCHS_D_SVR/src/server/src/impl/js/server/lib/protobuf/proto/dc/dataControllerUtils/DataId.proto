package dataControllerUtils.protobuf;

//The parameter full identity

message DataId {
extensions 9 to 109;

  required string parameterName = 1;
  optional string oid = 2; // oid can't and isn't filled by GPCCHS.
  required string catalog = 3;
  optional string comObject = 4;
  required uint32 sessionId = 5; // should be uint16
  optional uint32 domainId = 6; // should be uint16
  optional string url = 7; //for fds parameters
  optional string version = 8; //for fds parameters
}
