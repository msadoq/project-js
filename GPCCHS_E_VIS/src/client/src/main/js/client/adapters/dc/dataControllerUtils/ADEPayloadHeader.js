module.exports = {
  encode: ({providerId, comObjectType, instanceOid}) => ({
    providerId,
    comObjectType,
    instanceOid,
  }),
  decode: ({providerId, comObjectType, instanceOid}) => ({
    providerId,
    comObjectType,
    instanceOid,
  }),
};